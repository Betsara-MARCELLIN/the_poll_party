import 'package:flutter/material.dart';

const normalTextSize = 20.0;
const titleTextSize = 30.0;

const roundedBorder = BorderRadius.all(Radius.circular(15));
const circularedBorder = BorderRadius.all(Radius.circular(30));
